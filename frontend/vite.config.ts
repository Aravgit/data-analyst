import { defineConfig } from "vite";

// Vite is not required for Next.js, but this stub allows running Storybook/preview
// with vite-node if desired. It keeps the requested "Vite" presence minimal.
export default defineConfig({
  plugins: [],
});
